% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cnefe_export.R
\name{cnefe_export}
\alias{cnefe_export}
\title{Export CNEFE data to a persistent, optimised file}
\usage{
cnefe_export(
  code_muni,
  path,
  format = c("parquet", "csv", "csv.gz"),
  year = 2022,
  overwrite = FALSE,
  cache = TRUE,
  cache_dir = NULL,
  verbose = TRUE
)
}
\arguments{
\item{code_muni}{Integer. Seven-digit IBGE municipality code.}

\item{path}{Character. Directory to write into. Created if missing.}

\item{format}{Character. \code{"parquet"} (default), \code{"csv"} or \code{"csv.gz"}.}

\item{year}{Integer. The CNEFE data year. Currently only 2022 is supported.}

\item{overwrite}{Logical. Whether to replace an existing file. Defaults to
\code{FALSE}, which errors instead, since these files are expensive to produce.}

\item{cache}{Logical. Whether to use the package cache for the download.}

\item{cache_dir}{Character. Directory to use for cached downloads. If \code{NULL}
(default), the \code{CNEFETOOLS_CACHE_DIR} environment variable is used when it
is set, otherwise \code{\link[tools:R_user_dir]{tools::R_user_dir()}} with \code{which = "cache"}.}

\item{verbose}{Logical. Whether to print progress.}
}
\value{
The path to the written file, invisibly.
}
\description{
\code{cnefe_export()} downloads a municipality (or reuses the cache) and writes it
to a location and format of your choosing, so the data no longer depends on
the package cache being present or on the IBGE server being reachable.
}
\details{
The package cache is designed to be transient: it lives in a directory the
package manages, it holds the ZIP exactly as IBGE published it, and
\code{\link[=clear_cache_muni]{clear_cache_muni()}} is expected to empty it. That is the wrong shape for a
reproducible analysis that must still run in a year.

This function fills that gap. Point it at a project directory, a shared
volume or an external drive, choose a format, and use the resulting file
directly:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{path <- cnefe_export(2919207, "data/cnefe")
cnefe <- read_cnefe(file = path)
}\if{html}{\out{</div>}}

\code{read_cnefe()} accepts any file this function writes, and also the raw ZIP as
distributed by IBGE, so a file obtained by other means can be read without
the download step at all.

Parquet is the default because it is columnar, typed and compressed, which
makes it markedly smaller and faster to read than the published CSV, and
because it is the format Arrow and DuckDB both read natively.
}
\examples{
\donttest{
# Write a municipality to a project directory as Parquet
path <- cnefe_export(2929057, path = tempdir(), cache = FALSE)

# Read it back without touching the network
cnefe <- read_cnefe(file = path)
}

}
\seealso{
\code{\link[=read_cnefe]{read_cnefe()}}, which reads the result back through its \code{file}
argument, and \code{\link[=clear_cache_muni]{clear_cache_muni()}} for the transient cache.
}
